# Client configuration — copy to .env and fill in real values.

# Base URL of the backend API. Use the relative path in dev so the Vite proxy
# keeps requests same-origin (avoids CORS/preflight entirely).
VITE_API_URL=/api/v1
# Path used by the browser for real-time WebSocket connections.
VITE_SOCKET_URL=/
