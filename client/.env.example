# Client configuration — copy to .env and fill in real values.

# Base URL of the backend API (used directly in production builds).
VITE_API_URL=http://localhost:5000/api/v1
# Path used by the browser for real-time WebSocket connections.
VITE_SOCKET_URL=http://localhost:5000
